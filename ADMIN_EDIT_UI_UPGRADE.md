# تطوير واجهة تعديل الإدارة والموارد البشرية

تم استبدال تجربة التعديل القديمة المعتمدة على نوافذ المتصفح المنبثقة بشاشات تعديل احترافية مخصصة داخل التطبيق.

## ما تم تطويره

- تعديل الموظف: بيانات أساسية، الوظيفة، القسم، الهاتف، نوع الإقامة، والحالة.
- تعديل المشروع: العميل، الموقع، الإحداثيات، نطاق الحضور، والحالة.
- تعديل الوردية: بداية العمل، فتح/إغلاق الحضور، فتح/إغلاق الانصراف، والإغلاق التلقائي، والحالة.
- تغيير كلمة مرور الحساب: نافذة آمنة واضحة بدون عرض كلمة المرور القديمة.
- حذف `window.prompt` من مسارات تعديل الإدارة الأربعة.
- RTL كامل ونصوص عربية في تجربة المستخدم.
- حفظ واضح مع حالة انتظار وإلغاء.
- عرض تنبيهات وسياق مناسب قبل تعديل بيانات حساسة.
- تنبيه أن المشروع والوردية لا يتم العبث بتعييناتهما التاريخية من شاشة الموظف.
- اختبار regression مخصص للمحررات.

## التحقق

تم تشغيل `npm test` بالكامل ونجحت جميع الاختبارات، بما فيها اختبار واجهة التعديل الجديد.

ملاحظة: `next build` لم يتم تشغيله في بيئة العمل الحالية لعدم وجود dependencies كاملة محليًا؛ لذلك يجب اعتماد نتيجة Vercel بعد رفع الحزمة.
