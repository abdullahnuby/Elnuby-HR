
'use client';
import { useEffect,useState } from 'react'; import { api } from '@/lib/api';
const labels:any={active:'الموظفون على رأس العمل',present:'حضور اليوم',late:'تأخير اليوم',absent:'غياب اليوم',leave:'إجازات اليوم',permission:'أذونات اليوم',pendingLeaves:'إجازات معلقة',pendingPermissions:'أذونات معلقة',expiringDocs:'مستندات تحتاج متابعة',overdueApprovals:'اعتمادات متأخرة'};
export default function HRExecutiveDashboard({me,setSection}:any){const [d,setD]=useState<any>(null);const [busy,setBusy]=useState(true);useEffect(()=>{let on=true;(async()=>{try{const x=await api<any>('hr_executive_dashboard');if(on)setD(x)}catch(e:any){window.dispatchEvent(new CustomEvent('hr:toast',{detail:{message:e.message||'تعذر تحميل لوحة الموارد البشرية',type:'error'}}))}finally{if(on)setBusy(false)}})();return()=>{on=false}},[]);if(busy)return <section className="panel page-panel"><div className="empty-note">جاري تحميل لوحة الموارد البشرية…</div></section>;return <div className="hr-executive"><section className="panel page-panel"><div className="panel-head"><div><h2>لوحة الموارد البشرية</h2><p>ملخص تنفيذي للحضور والطلبات والتنبيهات في يوم العمل الحالي.</p></div><span className="status-badge success">اليوم</span></div><div className="report-grid report-month-kpis">{Object.entries(labels).map(([k,v])=><div key={k} onClick={()=>['pendingLeaves','pendingPermissions','overdueApprovals','expiringDocs'].includes(k)&&setSection(k==='overdueApprovals'?'approvals':k==='expiringDocs'?'notifications':k==='pendingLeaves'?'leaves':'permissions')} className="clickable-kpi"><span>{v as string}</span><b>{d?.[k]??0}</b></div>)}</div></section><section className="panel page-panel"><div className="panel-head"><div><h3>أهم ما يحتاج متابعة</h3><p>الأولوية للحالات التي تؤثر على الانضباط التشغيلي.</p></div></div><div className="empty-note">حالات الغياب اليوم: {d?.absent??0} • الاعتمادات المتأخرة: {d?.overdueApprovals??0} • المستندات التي تحتاج متابعة: {d?.expiringDocs??0}</div></section></div>}
